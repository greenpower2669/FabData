name: Android APK + AAB v0.10.3 (paused for v0.11)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paused while FabData v0.11.0 model upgrade is prepared."
